version: 0.2

      
phases:
  install:
    runtime-versions:
      java: openjdk8

    commands:
      - echo install
  pre_build:
    commands:
      - echo pre-build
      - command
  build:
    commands:
      - echo build
      - mvn package
  post_build:
    commands:
      - echo post build

artifacts:
  files:
    - target\aws-cicd-test.jar
  
 